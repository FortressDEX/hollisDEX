import { Price, CurrencyAmount, Currency } from "@uniswap/sdk-core";
export declare function formatTokenAmount(amount: CurrencyAmount<Currency> | undefined, sigFigs: number): string;
export declare function formatPrice(price: Price<Currency, Currency> | undefined, sigFigs: number): string;
