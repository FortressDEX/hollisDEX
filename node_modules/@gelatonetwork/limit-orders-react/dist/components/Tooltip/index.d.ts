import React from "react";
import { PopoverProps } from "../Popover";
interface TooltipProps extends Omit<PopoverProps, "content"> {
    text: string;
}
interface TooltipContentProps extends Omit<PopoverProps, "content"> {
    content: React.ReactNode;
}
export default function Tooltip({ text, ...rest }: TooltipProps): JSX.Element;
export declare function TooltipContent({ content, ...rest }: TooltipContentProps): JSX.Element;
export declare function MouseoverTooltip({ children, ...rest }: Omit<TooltipProps, "show">): JSX.Element;
export declare function MouseoverTooltipContent({ content, children, ...rest }: Omit<TooltipContentProps, "show">): JSX.Element;
export {};
