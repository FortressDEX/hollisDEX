import React from 'react';
interface ModalProps {
    isOpen: boolean;
    onDismiss: () => void;
    children?: React.ReactNode;
    overlayBG?: string;
}
export default function Modal({ isOpen, onDismiss, children, overlayBG }: ModalProps): JSX.Element;
export {};
